{
  "name": "gsplat-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Local backend: photos -> COLMAP -> Brush -> Gaussian splat .ply",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1"
  }
}
